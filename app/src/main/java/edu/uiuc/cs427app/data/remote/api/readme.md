##  APIs
Declarations of interactions with external 3rd-party APIs.

Note that the default return value uses Call<T> but GuavaCallAdapterFactory is added to allow for
ListenableFuture<Response<T>> to be used to align with Room.

## Resources
- https://square.github.io/retrofit/declarations/